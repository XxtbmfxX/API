version: "3.9"
services:
  fastapi-app:
    build: .
    container_name: fastapi-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app/credenciales.json:/app/app/credenciales.json:ro
    restart: unless-stopped